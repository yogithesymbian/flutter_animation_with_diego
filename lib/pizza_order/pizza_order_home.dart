import 'package:flutter/material.dart';

class PizzaOrderHome extends StatelessWidget {
  const PizzaOrderHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("New Orleans Pizza"),
      ),
      body: null,
    );
  }
}
